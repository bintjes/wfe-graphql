type PublicationTask{
    id: Int!
    status_id: Int
    position: Int
    product: String
    service_url: String
    publication_id: Int
    message: String
    last_run: String
    transcodingJob: TranscodingJob
    publicationTaskStatus : PublicationStatus
}